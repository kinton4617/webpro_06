<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <title>文明一覧</title>
    <style>
        body {
            font-family: sans-serif;
            background-color: #f9f5f0;
            color: #4a3b32;
            padding: 20px;
        }

        h1 {
            text-align: center;
            color: #8b5a2b;
            border-bottom: 2px solid #8b5a2b;
            display: inline-block;
            padding-bottom: 5px;
        }

        .header-area {
            text-align: center;
            margin-bottom: 20px;
        }

        /* リンクボタンのデザイン */
        .btn-link {
            display: inline-block;
            padding: 10px 20px;
            background-color: #8b5a2b;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin: 0 5px;
            font-weight: bold;
        }

        .btn-link:hover {
            background-color: #6d4621;
        }

        .btn-top {
            background-color: #666;
        }

        .btn-top:hover {
            background-color: #444;
        }

        /* テーブルデザイン */
        table {
            width: 100%;
            max-width: 900px;
            margin: 0 auto;
            border-collapse: collapse;
            background: white;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        th {
            background-color: #8b5a2b;
            color: white;
            padding: 12px;
        }

        td {
            border-bottom: 1px solid #ddd;
            padding: 12px;
            text-align: center;
            vertical-align: middle;
        }

        tr:hover {
            background-color: #f1e3d3;
        }

        .thumb-img {
            width: 80px;
            height: auto;
            border-radius: 4px;
            object-fit: cover;
        }

        .action-links a {
            color: #8b5a2b;
            margin: 0 5px;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div class="header-area">
        <h1>文明一覧システム</h1>
        <br>
        <a href="/civilization/create" class="btn-link">＋ 新規追加</a>
        <a href="/" class="btn-link btn-top">トップメニューへ</a>
    </div>

    <table border="0">
        <tr>
            <th>ID</th>
            <th>画像</th>
            <th>文明名</th>
            <th>操作</th>
        </tr>
        <% data.forEach((item)=> { %>
            <tr>
                <td>
                    <%= item.id %>
                </td>
                <td>
                    <% if(item.image) { %>
                        <img src="<%= item.image %>" class="thumb-img" alt="画像">
                        <% } else { %>
                            <span style="color:#ccc; font-size:0.8em;">No Image</span>
                            <% } %>
                </td>
                <td style="font-weight:bold; font-size:1.1em;">
                    <a href="/civilization/detail/<%= item.id %>" style="color:#4a3b32; text-decoration:none;">
                        <%= item.name %>
                    </a>
                </td>
                <td class="action-links">
                    <a href="/civilization/detail/<%= item.id %>">詳細</a> |
                    <a href="/civilization/edit/<%= item.id %>">編集</a> |
                    <a href="/civilization/delete/<%= item.id %>">削除</a>
                </td>
            </tr>
            <% }); %>
    </table>
</body>

</html>