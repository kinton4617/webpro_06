<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <title>
        <%= data.name %>の詳細
    </title>
    <style>
        body {
            font-family: "Helvetica Neue", Arial, sans-serif;
            background-color: #e8f5e9;
            color: #1b5e20;
            padding: 20px;
        }

        .card {
            max-width: 600px;
            margin: 0 auto;
            background: white;
            border: 1px solid #a5d6a7;
            border-radius: 8px;
            padding: 30px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        h1 {
            color: #2e7d32;
            text-align: center;
            border-bottom: 2px solid #a5d6a7;
            padding-bottom: 10px;
        }

        .img-box {
            text-align: center;
            margin: 20px 0;
        }

        .img-box img {
            max-width: 100%;
            border-radius: 4px;
            border: 2px solid #c8e6c9;
        }

        ul {
            list-style: none;
            padding: 0;
        }

        li {
            padding: 10px;
            border-bottom: 1px dashed #c8e6c9;
            font-size: 1.1em;
        }

        .label {
            font-weight: bold;
            color: #388e3c;
            display: inline-block;
            width: 80px;
        }

        .footer {
            text-align: center;
            margin-top: 30px;
        }

        .btn {
            padding: 10px 20px;
            background: #2e7d32;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            margin: 0 5px;
        }
    </style>
</head>

<body>
    <div class="card">
        <h1>
            <%= data.name %>
        </h1>

        <% if(data.image) { %>
            <div class="img-box"><img src="<%= data.image %>"></div>
            <% } %>

                <ul>
                    <li><span class="label">ID</span>
                        <%= data.id %>
                    </li>
                    <li><span class="label">期間</span>
                        <%= data.period %>
                    </li>
                    <li><span class="label">地域</span>
                        <%= data.region %>
                    </li>
                    <li><span class="label">特徴</span>
                        <%= data.feature %>
                    </li>
                </ul>

                <div class="footer">
                    <a href="/era/edit/<%= data.id %>" class="btn">編集する</a>
                    <a href="/era/list" class="btn" style="background:#555;">一覧に戻る</a>
                </div>
    </div>
</body>

</html>