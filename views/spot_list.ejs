<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <title>観光地一覧</title>
    <style>
        /* 全体の基本設定（ブルー系） */
        body {
            font-family: "Segoe UI", Tahoma, sans-serif;
            background-color: #e1f5fe;
            color: #333;
            padding: 20px;
        }

        /* タイトルデザイン */
        h1 {
            text-align: center;
            color: #0277bd;
            border-bottom: 2px solid #0277bd;
            display: inline-block;
            padding-bottom: 5px;
        }

        /* ボタンエリア */
        .header-area {
            text-align: center;
            margin-bottom: 20px;
        }

        .btn-link {
            display: inline-block;
            padding: 10px 20px;
            background-color: #0277bd;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin: 0 5px;
            font-weight: bold;
        }

        .btn-link:hover {
            background-color: #01579b;
        }

        .btn-top {
            background-color: #546e7a;
        }

        /* トップへ戻るボタンは少し色を変える */
        .btn-top:hover {
            background-color: #37474f;
        }

        /* テーブルデザイン */
        table {
            width: 100%;
            max-width: 1000px;
            margin: 0 auto;
            border-collapse: collapse;
            background: white;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        th {
            background-color: #0277bd;
            color: white;
            padding: 12px;
        }

        td {
            border-bottom: 1px solid #ddd;
            padding: 12px;
            text-align: center;
            vertical-align: middle;
        }

        /* 行にマウスを乗せた時の色 */
        tr:hover {
            background-color: #b3e5fc;
        }

        /* 画像サムネイル */
        .thumb-img {
            width: 80px;
            height: 50px;
            object-fit: cover;
            border-radius: 4px;
            border: 1px solid #ccc;
        }

        /* 操作リンク */
        .action-links a {
            color: #0277bd;
            margin: 0 5px;
            font-weight: bold;
            text-decoration: none;
        }

        .action-links a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="header-area">
        <h1>世界の観光地一覧</h1>
        <br>
        <a href="/spot/create" class="btn-link">＋ 新規追加</a>
        <a href="/" class="btn-link btn-top">トップメニューへ</a>
    </div>

    <table>
        <tr>
            <th>ID</th>
            <th>画像</th>
            <th>観光地名</th>
            <th>国名</th>
            <th>種別</th>
            <th>操作</th>
        </tr>
        <% data.forEach((item)=> { %>
            <tr>
                <td>
                    <%= item.id %>
                </td>
                <td>
                    <% if(item.image) { %>
                        <img src="<%= item.image %>" class="thumb-img" alt="画像">
                        <% } else { %>
                            <span style="color:#ccc; font-size:0.8em;">No Image</span>
                            <% } %>
                </td>
                <td style="font-weight:bold; font-size:1.1em; text-align: left; padding-left: 20px;">
                    <a href="/spot/detail/<%= item.id %>" style="color:#01579b; text-decoration:none;">
                        <%= item.name %>
                    </a>
                </td>
                <td>
                    <%= item.country %>
                </td>
                <td>
                    <%= item.type %>
                </td>
                <td class="action-links">
                    <a href="/spot/edit/<%= item.id %>">編集</a> |
                    <a href="/spot/delete/<%= item.id %>">削除</a>
                </td>
            </tr>
            <% }); %>
    </table>
</body>

</html>